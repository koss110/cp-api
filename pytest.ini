[pytest]
filterwarnings =
    # starlette 0.36.x (bundled with fastapi 0.111.0) imports python-multipart
    # via the legacy `import multipart` name. This is a transitive third-party
    # warning we cannot fix without upgrading fastapi/starlette.
    ignore::PendingDeprecationWarning:starlette
