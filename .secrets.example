# Copy to .secrets and fill in your values.
# Used by act to simulate GitHub Actions secrets locally.
# .secrets is gitignored — never commit real credentials.
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=us-east-2
INFRA_REPO_TOKEN=
INFRA_REPO=yourorg/cp-infra
